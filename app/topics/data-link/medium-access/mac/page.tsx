import { MacVisualizerScreen } from "@/components/visualizer/MacVisualizerScreen";

export default function Page() {
  return <MacVisualizerScreen path="/topics/data-link/medium-access/mac" title="The MAC Problem" blurb="What happens when every station talks at once — and the three ways out." operation="macProblem" />;
}
