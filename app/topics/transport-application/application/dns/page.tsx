import { LadderVisualizerScreen } from "@/components/visualizer/LadderVisualizerScreen";

export default function Page() {
  return <LadderVisualizerScreen path="/topics/transport-application/application/dns" title="DNS" blurb="Root, TLD, authoritative: turning a name into an address." operation="dns" />;
}
