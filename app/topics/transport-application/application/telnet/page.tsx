import { LadderVisualizerScreen } from "@/components/visualizer/LadderVisualizerScreen";

export default function Page() {
  return <LadderVisualizerScreen path="/topics/transport-application/application/telnet" title="Telnet" blurb="A remote terminal over TCP, one echoed keystroke at a time." operation="telnet" />;
}
